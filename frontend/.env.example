# Supabase (frontend) — Vite inlines these at build time, so they must be
# publishable values only. Never put the service_role / secret key here.
#
# Supabase dashboard → Connect → App Frameworks, or Settings → API Keys.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
